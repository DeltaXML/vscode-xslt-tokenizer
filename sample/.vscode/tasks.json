{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "xslt",
			"label": "xslt: Saxon Transform (New)",
			"saxonJar": "${config:xpe.tasks.saxonJar}",
			"xsltFile": "${file}",
			"xmlSource": "${file}",
			"resultPath": "${workspaceFolder}/xslt-out/result1.xml",
			"group": {
				"kind": "build"
			},
			"problemMatcher": [
				"$saxon-xslt"
			]
		}
	]
}